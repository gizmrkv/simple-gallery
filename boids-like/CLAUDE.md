# CLAUDE.md

このファイルは`simple-boids-like`リポジトリで作業するときのプロジェクト固有の
指示。別のセッション・別の担当（人間・AI問わず）が引き継いでも同じ前提で
作業を再開できるようにするためのもの。

## このプロジェクトは何か

boidの群れを**1つの共有プログラム**で操作し、各boidが「自分の速度＋視界内の
他boid/資源/拠点の相対位置・相対速度」しか知覚できない（絶対位置は一切
わからない）という制約のもとで、資源収集・基地拡張・防衛などを解いていく
ゲームのアイデアを検証している。現在は**Phase 0: アイデア検証プロトタイプ**
の段階。

- 全体像・現状: [docs/roadmap.md](docs/roadmap.md)
- エンジンの技術仕様: [docs/engine-spec.md](docs/engine-spec.md)
- 人間向けの概要・セットアップ: [README.md](README.md)

## 重要な設計制約（勝手に変えないこと）

### boidに加速度・慣性を持たせない

boidが持つ状態は**位置と速度のみ**。プログラムは毎tick「次の速度ベクトル」を
直接返し（大きさは`PHYSICS.maxSpeed`でクランプ）、次tickの位置は
`pos = pos + vel` で決まる。加速度・慣性・dtスケーリングは導入しない。

過去に一度「boidsっぽさ」を出そうとして`vel += accel * dt`という慣性モデルを
勝手に実装してしまい、ユーザーから明確に指摘・修正依頼を受けた。理由は
「視界制限による協調の難しさ」だけで十分ゲーム性が成立する見込みで、そこに
慣性による操作の難しさを追加すると意図した以上に難易度が上がってしまうため。
**この種の「頼まれていない仕様を先回りして追加してしまう」ミスを、他の
メカニクス（燃料・handoffなど）でも繰り返さないこと。** 新しいゲームメカニクス
を追加するときは、それが本当にユーザーから要求されたものかを一度確認してから
実装する。

### 独自言語・ファイル実行は今はやらない

Phase 0では意図的に未実装。boidプログラムはホスト言語(TypeScript)の関数
（`src/programs/*.ts`）としてそのまま書く。独自言語の設計はPhase 2（Phase 1で
TS関数を書く中で感じた不便を先に洗い出してから）まで着手しない。

## コミットルール

- **コミットはユーザーに都度確認を取らずに行ってよい。** このプロジェクトでは
  標準の許可として扱う。ただし`git push`などリモートや他のリポジトリに影響
  する操作は引き続き別途確認を取ること。
- **意味のある単位ごとにコミットする**（雛形構築／コアエンジン実装／機能追加
  ／バグ修正、を分ける）。1機能・1修正=1コミットが目安で、細かすぎず粗すぎず。
  複数の異なる目的の変更が作業ツリーに積み重なっている場合、まとめて1コミット
  にせず、後からでも意味のある単位に分割してコミットする。
- コミット前に必ず下記「変更の検証方法」を実行する。動作未確認のまま
  コミットしない。
- コミットメッセージは「何をしたか」より「なぜそうしたか」を書く（背景・
  動機、見つかった不具合ならその原因）。「何をしたか」はdiffを見れば
  わかるので繰り返さない。

## 変更の検証方法

1. **型チェック**: `npx tsc --noEmit`
2. **ビルド**: `npx vite build`（確認後 `dist/` は削除してよい、gitignore済み）
3. **ヘッドレス動作検証**（推奨・最重要）:
   `npx tsx`で各シナリオの`createWorld()` → `step(world, program)` →
   `checkWin(world)`を直接ループさせ、実際に勝利条件を満たすか・何tickかかるか
   を確認する。乱数要素があるシナリオ（formation/relay/frontier）は複数回
   （目安10回程度）実行して安定性も見ること。
   **ブラウザの見た目だけでは気づけない不具合をここで洗い出せる。** 実際に
   このプロジェクトで4件見つかっている: (1) 燃料をエンジン全体に追加した際、
   一部のシナリオのプログラムが燃料切れを考慮しておらず立ち往生した、
   (2) 陣形維持のバネ力に減衰がなく振動が発散した、(3) リレーシナリオの
   距離設定が燃料の片道分すら超えており原理的に解けなかった、(4) frontier.ts
   で分離力（Separation）のみによる探索ロジックに変更した際、群れの中心付近で
   「アンカーは視界に入っているが密集による反発でinteractRadius内に入れない」
   板挟み状態に陥り、全boidが燃料切れで永久に孤立するデッドロックがheadless
   10回中3〜4回の頻度で発生した（詳細は`docs/roadmap.md`参照）。型チェックも
   ビルドも通っていたが、いずれも実際にシミュレーションを走らせるまで
   気づけなかった。
4. **ブラウザ実機確認**: `npm run dev`で起動し、実際に画面を見て
   コンソールエラーが出ていないか確認する。

   > **sandbox環境の制約**: headless chromiumに必要なシステムライブラリが
   > 無く、sudoも使えずインストールできない場合がある。その場合は
   > 「見た目の確認はできなかった」と正直に報告し、ヘッドレス検証で代替する。
   > できないことをできたと報告しない。

## 環境の既知の注意点

- **WSL2でdev serverが開けない場合**: `package.json`の`dev`スクリプトは
  `vite --host`にしてある。`--host`を外すとViteが`[::1]`（IPv6ループバック）
  だけにバインドされ、WindowsからWSL2へのポートフォワーディング（IPv4ベース）
  が届かずブラウザで開けなくなる。`--host`を外さないこと。
